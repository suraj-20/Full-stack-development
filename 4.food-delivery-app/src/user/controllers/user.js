
// api/user/register
module.exports.register = (req, res)=>{
    // read req.body.
    // DB calls.
    res.write("This is register request");
}

// api/user/login
module.exports.login = (req, res)=>{
    // read req.body.
    // DB calls.
    res.write("This is login request");
}
